name: "CodeQL Analysis"

on:
  push:
    paths-ignore: 
    - '**/*.md'
    - '**/*.txt'
    - '**/*.dox'

  pull_request:
    paths-ignore: 
    - '**/*.md'
    - '**/*.txt'
    - '**/*.dox'

jobs:
  codeql:
    uses: ArielSAdams/cFS/.github/workflows/codeql-reusable.yml@Test
    with: 
      component-path: cfe
      make: make -j8
      test: true
