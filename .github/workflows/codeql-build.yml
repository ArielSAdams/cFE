name: "CodeQL Analysis"

on:
  push:
  pull_request:

jobs:
  codeql:
    uses: ArielSAdams/cFS/.github/workflows/codeql-reusable.yml@test-paths-ignore
    with: 
      component-path: cfe
      make: make -j8
      test: true
